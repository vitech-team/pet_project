version: '2'
services:
  web:
    build: .
    ports:
    - "8080:8080"
    volumes:
    - /tmp:/tmp
  mypostgres:
    image: postgres
    ports:
    - "5432:5432"
    environment:
    - POSTGRES_PASSWORD=password
    - POSTGRES_USER=postgres
    - POSTGRES_DB=mydb